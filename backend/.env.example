# API Port Settings
PORT=8000

# MongoDB Connection String (Atlas URL)
# Replace with your connection string. Format: mongodb+srv://username:password@cluster.mongodb.net/
# If left empty, the application will automatically fall back to a high-fidelity local Mock Database.
MONGODB_URI=
